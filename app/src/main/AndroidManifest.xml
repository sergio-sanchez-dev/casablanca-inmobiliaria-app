<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.READ_CALENDAR"/>
    <uses-permission android:name="android.permission.WRITE_CALENDAR"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Https">

        <!-- Activities -->

        <activity android:name=".EntrandoActivity"/>
        <activity android:name=".Pantalla2Activity"/>
        <activity android:name=".CrearCuentaActivity"/>
        <activity android:name=".NotificacionesActivity"/>
        <activity android:name=".PerfilActivity"/>
        <activity android:name=".TareasActivity"/>
        <activity android:name=".DashboardActivity"/>
        <activity android:name=".CaptacionActivity"/>
        <activity android:name=".EncargoActivity"/>
        <activity android:name=".VisitasActivity"/>
        <activity android:name=".FirmaActivity"/>
        <activity android:name=".SenalActivity"/>
        <activity android:name=".PropuestaActivity"/>
        <activity android:name=".ContratoActivity"/>
        <activity android:name=".CalendarioActivity"/>
        <activity android:name=".SetupActivity"/>

        <!-- Launcher -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Receiver -->
        <receiver
            android:name=".NotificacionReceiver"
            android:exported="false"/>

        <!-- 🔥 ESTE ES EL CORRECTO LUGAR -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

    </application>

</manifest>